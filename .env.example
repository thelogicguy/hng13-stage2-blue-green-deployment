# Container Images
BLUE_IMAGE=yimikaade/wonderful:devops-stage-two
GREEN_IMAGE=yimikaade/wonderful:devops-stage-two

# Active Pool (blue or green)
ACTIVE_POOL=blue

# Release Identifiers
RELEASE_ID_BLUE=blue-v1.0.0
RELEASE_ID_GREEN=green-v1.0.0

# Application Port (internal container port)
PORT=3000

# ============================================================================
# OBSERVABILITY & ALERTS CONFIGURATION
# ============================================================================

# Slack Webhook URL for alerts
# How to get this:
# 1. Go to https://api.slack.com/apps
# 2. Create a new app or select existing app
# 3. Go to "Incoming Webhooks" and activate it
# 4. Click "Add New Webhook to Workspace"
# 5. Select a channel and copy the webhook URL
SLACK_WEBHOOK_URL=your-slack-webhook-url-here

# Error Rate Threshold (percentage)
# Alerts when 5xx error rate exceeds this percentage over the sliding window
# Default: 2 (means 2% error rate)
ERROR_RATE_THRESHOLD=2

# Sliding Window Size (number of requests)
# Number of recent requests to consider for error rate calculation
# Default: 200 requests
WINDOW_SIZE=200

# Alert Cooldown (seconds)
# Minimum time between alerts of the same type to prevent spam
# Default: 300 seconds (5 minutes)
ALERT_COOLDOWN_SEC=300

# Maintenance Mode
# Set to 'true' to suppress all alerts during planned maintenance
# Can also be enabled by creating /app/state/maintenance.flag in the watcher container
# Default: false
MAINTENANCE_MODE=false